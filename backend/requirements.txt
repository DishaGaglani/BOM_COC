fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# Local (open-source) document parsing — no external API calls.
# [pdf] pulls in unstructured-inference (layout model) + pdf2image + pytesseract for OCR.
# [xlsx] pulls in openpyxl. [docx] pulls in python-docx. [csv] is lightweight.
unstructured[pdf,xlsx,docx,csv]==0.15.13

# unstructured-inference's table-structure model (Table Transformer, via
# transformers) has no upper version bound in 0.15.13, so pip resolves
# whatever transformers is newest at install time — which has since
# restructured its internals and breaks that model's import chain with
# `NameError: name 'torch' is not defined`. Pin to the version current when
# unstructured 0.15.13 shipped.
transformers==4.44.2
# CPU-only torch (installed separately in the Dockerfile via the PyTorch CPU
# index, see below) — required for the same table-structure model.

# unstructured 0.15.13 pins pdfminer.six loosely; recent pdfminer.six
# releases renamed PSSyntaxError to PDFSyntaxError, which breaks
# unstructured's pdf partitioner import. Pin to the compatible version.
pdfminer.six==20240706

# Rule-based parameter extraction / validation.
rapidfuzz==3.9.7
python-dateutil==2.9.0.post0

# COC PDF highlighting.
PyMuPDF==1.24.10
